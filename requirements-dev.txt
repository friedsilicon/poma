# Development dependencies for YANG modeling
# Install with: pip install -r requirements-dev.txt

# YANG validation and processing tools
pyang>=2.5.3          # YANG validator, converter, and code generator
# libyang>=2.0.0        # Python bindings for libyang library (requires system libyang)

# Documentation generation (optional - for docs development)
mkdocs-material>=9.0.0    # Material theme for MkDocs
mkdocs-git-revision-date-localized-plugin>=1.2.0  # Git revision dates

# IMPORTANT: libyang Python bindings require system-level installation first!
# Install system libyang before running: pip install libyang
# See YANG_TOOLS.md for system installation instructions

# NOTE: yanggui is not pip-installable
# See installation instructions below or in README.md

# Additional YANG tools
yangson>=1.4.0        # JSON/YANG data processing
pyangbind>=0.8.1      # Python bindings for YANG models

# Note: libyang Python bindings are now included above

# Development and testing tools
pytest>=7.0.0         # Testing framework
black>=22.0.0         # Code formatter
flake8>=4.0.0         # Linting
mypy>=0.991           # Type checking

# Documentation tools
sphinx>=4.0.0         # Documentation generator
sphinx-rtd-theme>=1.0.0  # Read the Docs theme for Sphinx

# Jupyter for interactive development
jupyter>=1.0.0        # Jupyter notebooks
ipykernel>=6.0.0      # Jupyter kernel

# Network automation libraries (useful for YANG work)
ncclient>=0.6.13      # NETCONF client
netmiko>=4.0.0        # Network device automation
requests>=2.28.0      # HTTP requests

# XML/JSON processing
lxml>=4.9.0           # XML processing
xmltodict>=0.13.0     # XML to dict conversion
jsonschema>=4.0.0     # JSON schema validation

# Additional YANG tools installation:
# 
# yanglint CLI (optional, for command-line usage):
# macOS: brew install libyang
# Ubuntu: sudo apt-get install libyang-tools
# Note: Python bindings (libyang package) are installed via pip above
# 
# yanggui:
# See: https://github.com/alliedtelesis/yanggui
# May require manual installation
